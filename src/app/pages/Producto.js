import React from 'react'

const Producto = () => {
    const renderContent = () => {
        return (
            <div>
                PRODUCT
            </div>
        );
    };

    return renderContent();
};

export default Producto
